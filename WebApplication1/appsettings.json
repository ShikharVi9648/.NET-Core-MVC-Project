{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    //1
    "Defaultconn": "Server=localhost; Database=shikharYT; Trusted_Connection=true;TrustServerCertificate=true;Encrypt=false;"
  },
  "AllowedHosts": "*"
}


/*
  1st we create a model name user.cs and add what required 
  2st we have to create connection string in appsetting name defaultconn
  3then we make data name appdbcontext to add the table in db 
  4 now go in program.cs to connect the connection string with db 
  5 then we make migration file using add-migration DbInit then update database
  6 now we make authcontroller as same name in view folder make login and register
  7 for default page we go in program.cs and make default login action login 
  8 now add html in login and register and use asp-action for moving from one to another 
  add in form to add the dataa in database in form tag add asp-action=method of creating user asp-controller method=post
  9 now we make dto which work between request and response as database first user connect with dto then dto update the real database
  10 making dto as like mode user.cs same thing copy into dto 
  11 now we import dto using @model dto in login and register and use them as name ="username" asp-action ="Username" add in all
  12 now for creating ueer we make function in authcontroller and use usedto dto in parameter 
  13 now inject dbcontext as primati contructor in authcontroller as authcontroller(appdbcontext context ) : controller

  14 New user creation()
  now for creating new user 
    existing user = await context.User.fistordefaultasync(u=> u.Email == dto.Email)
  then return redirecttoaction(Login)

  14 if user existed in db then we use viewbad.errormsg()
  for success add tempData["successmessage"]= ""
  and add in login as viewbag.success= this or  type some thing 
  now for empty field we create a check like all the field should be in form we use if condition wher dto == null || string.isnullorempty
  then viewbag.errormesssage=""; 

 15  loginUser()

  create function name loginUser()
  after creating all this go in login.cshtml and write asp-for for each field and in form asp-action as LoginUser
  after login we have to go for dashboard page to make controller for dash and same in view make folder dash in index.cshtml


 16 dashboard table creation
  for creation dashtable first we create model of dash then add in to db context and add html in index make dto as well
  now after creating run build and nuget add-migration to add table in data base then update
  after creating this go in controller make primary constructor and use 
  var list = context.Products.select().ToList();
  add Product dto as model in index.cshtml 
  now making product dto use select method in list and add all the table details from the model 

  make index as upword i write

  now create product 
  then add into index 
  then delete product 
  now edit
  make a new form update
  then add the select() all the field
  make updaeproduct ()
  find data using id then data.name =dto.name use and then use update method then save changes 


  Jwt install package of microdoft jwt bearers 
  then make service for this in program.cs




*/
